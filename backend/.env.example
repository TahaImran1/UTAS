# ── UTAS Server Configuration ────────────────────────────────────────────────
# This file is copied to .env on first launch if no .env exists.
# Edit .env in %APPDATA%\UTAS\ to configure your environment.

# Server port (ZKTeco ADMS devices must push to this port)
SERVER_PORT=4370

# ── Pull Engine (pyzk) ──────────────────────────────────────────────────────
PULL_SYNC_INTERVAL=20
PULL_AUTO_START=true
PULL_AUTO_DELETE=false
PULL_TIMEOUT=15

# ── FK Bridge ───────────────────────────────────────────────────────────────
FK_BRIDGE_URL=http://127.0.0.1:5001
FK_TCP_BACKOFF_SEC=300
FK_TCP_MAX_FAILS=2

# ── Auth & Security ─────────────────────────────────────────────────────────
ADMIN_USER=admin
# Default password: admin123  (change after first login via Settings)
ADMIN_PASS_HASH=$2b$12$HsX55kfXFpUGdXatBVl48OaojBm56HI0ZntORMuokZ0ISM/is.kh2
JWT_SECRET=utas-secret-change-me
